diff --git a/node_modules/@react-navigation/native/lib/module/useLinking.js b/node_modules/@react-navigation/native/lib/module/useLinking.js
index 0933348..860424d 100644
--- a/node_modules/@react-navigation/native/lib/module/useLinking.js
+++ b/node_modules/@react-navigation/native/lib/module/useLinking.js
@@ -182,7 +182,18 @@
       // When browser back/forward is clicked, we first need to check if state object for this index exists
       // If it does we'll reset to that state object
       // Otherwise, we'll handle it like a regular deep link
+      const currentState = navigation.getRootState();
+      const currentPath = currentState ? getPathFromStateRef.current(currentState, configRef.current) : undefined;
       const record = history.get(index);
+      const browserBackGuard = window.expensifyBrowserBackGuard;
+      if (path !== currentPath && currentPath && currentState && (browserBackGuard?.shouldBlock?.() ?? false)) {
+        history.push({
+          path: currentPath,
+          state: currentState
+        });
+        browserBackGuard.onBlock?.();
+        return;
+      }
       if (record?.path === path && record?.state) {
         navigation.resetRoot(record.state);
         return;
diff --git a/node_modules/@react-navigation/native/src/useLinking.tsx b/node_modules/@react-navigation/native/src/useLinking.tsx
index a33b493..2e1077f 100644
--- a/node_modules/@react-navigation/native/src/useLinking.tsx
+++ b/node_modules/@react-navigation/native/src/useLinking.tsx
@@ -71,6 +71,13 @@
 
 const linkingHandlers: symbol[] = [];
 
+type BrowserBackGuardWindow = Window & {
+  expensifyBrowserBackGuard?: {
+    shouldBlock?: () => boolean;
+    onBlock?: () => void;
+  };
+};
+
 type Options = LinkingOptions<ParamListBase>;
 
 export function useLinking(
@@ -212,7 +219,27 @@
       // When browser back/forward is clicked, we first need to check if state object for this index exists
       // If it does we'll reset to that state object
       // Otherwise, we'll handle it like a regular deep link
+      const currentState = navigation.getRootState();
+      const currentPath = currentState
+        ? getPathFromStateRef.current(currentState, configRef.current)
+        : undefined;
       const record = history.get(index);
+      const browserBackGuard = (window as BrowserBackGuardWindow)
+        .expensifyBrowserBackGuard;
+
+      if (
+        path !== currentPath &&
+        currentPath &&
+        currentState &&
+        (browserBackGuard?.shouldBlock?.() ?? false)
+      ) {
+        history.push({
+          path: currentPath,
+          state: currentState,
+        });
+        browserBackGuard.onBlock?.();
+        return;
+      }
 
       if (record?.path === path && record?.state) {
         navigation.resetRoot(record.state);
